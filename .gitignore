node_modules/
.env
.DS_Store
*.log

# Persisted position store (generated, machine-local)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Tuner checkpoint / results (generated)
tuner-checkpoint.json
tuner-results.json
