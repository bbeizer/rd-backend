node_modules/
.env
.DS_Store
*.log

# Persisted position store (generated, machine-local)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Tuner checkpoint / results (generated)
tuner-checkpoint.json
tuner-results.json

# Selfplay data (generated; large, regenerable)
data/selfplay/*.jsonl

# NN weight variants (canonical mlp_weights.json / model.onnx are tracked; A/B copies are local)
mlp_weights_*.json
model_*.onnx

# Local Claude config / agent state
.claude/

# Legacy artifact from earlier training round
weights.json
